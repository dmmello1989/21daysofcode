import { Link } from "react-router-dom"

export const List = () => {
  return (
    <div>
      This is the list.

      if ya wanna go back to the home, click <Link to="/">here</Link>.
    </div>
  )
}